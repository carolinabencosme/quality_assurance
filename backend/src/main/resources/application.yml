spring:
  application:
    name: inventory-api

server:
  port: ${SERVER_PORT:8080}

# /actuator/health debe permanecer publico (Docker healthcheck, Fase 2 Security)
management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: when_authorized
