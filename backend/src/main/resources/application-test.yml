# Perfil test - mvn test / CI (Fase 0, sin BD)
spring:
  main:
    banner-mode: off

logging:
  level:
    root: WARN
    com.company.inventory: INFO

management:
  endpoint:
    health:
      show-details: never
