# Staging / preview — Plan v3.0 (Fases 5-6)
# Misma base que dev + observabilidad. SonarQube/Jenkins en Fase 6.
#
# Uso:
#   docker compose -f docker-compose.dev.yml -f docker-compose.observability.yml up -d --build
#
# Puertos: frontend 3000, backend 8080, grafana 3001, prometheus 9090,
#          loki 3100, tempo 3200, alloy 4317/4318, alertmanager 9093
