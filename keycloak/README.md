# Keycloak — realm de inventario

- **`realm-export.json`**: importado al arrancar el contenedor (`start-dev --import-realm`).
- Realm: `inventory-realm`
- Cliente frontend: `inventory-frontend`
- Usuarios de prueba: ver [`docs/security-model.md`](../docs/security-model.md)

Puerto host: **8081** → contenedor **8080**.
