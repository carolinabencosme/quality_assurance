# Defects and Risks Log

| ID | Title | Severity | Module | Description | Repro steps | Expected | Obtained | Fix / workaround | Evidence | Status |
|---|---|---|---|---|---|---|---|---|---|---|
| DEF-001 | Keycloak redirect to wrong port | High | Auth | Incorrect public Keycloak URL can redirect browser to backend port 8080. | Start app with wrong `NEXT_PUBLIC_KEYCLOAK_URL`. | Redirect to 8081. | 404/invalid auth on 8080. | Keep `NEXT_PUBLIC_KEYCLOAK_URL=http://localhost:8081`. | E2E helper error message. | Mitigated |
| DEF-002 | JWKS internal/external mismatch | High | Backend security | Backend in Docker must use internal Keycloak URL for JWKS while JWT issuer remains localhost. | Use localhost JWKS from backend container. | JWT validates. | 401 due JWKS fetch failure. | Keep `KEYCLOAK_JWKS_URI=http://keycloak:8080/...`. | Compose config. | Mitigated |
| DEF-003 | Testcontainers on Windows socket | Medium | Backend tests | Docker Desktop named pipe can be missed by Java/Testcontainers. | Run `mvn verify` on Windows without override. | Integration tests run. | Skips/failures depending Docker setup. | Set `TESTCONTAINERS_DOCKER_SOCKET_OVERRIDE=//./pipe/docker_engine`. | Testing guide. | Documented |
| DEF-004 | Loki/Tempo readiness can be flaky | Medium | Observability | First start may take longer than smoke timeout. | Start full observability stack cold. | Smoke green. | Temporary failures. | Healthcheck retries and documented rerun. | Observability guide. | Documented |
| DEF-005 | GitHub Actions duplicate trigger risk | Low | CI/CD | Multiple workflows can run on same push and consume time. | Push to `presentacion`. | Clear CI split. | Long total runtime. | Heavy workflows are separated and full QA is manual/defendible. | Workflows. | Accepted |
| DEF-006 | Heavy security scans depend on network | Medium | DevSecOps | Dependency Check and Docker scans download databases/images. | Run scans offline. | Report generated. | Download failure. | CI artifacts/manual run; documented as heavy scan. | Security docs. | Documented |
| DEF-007 | Local running backend returned 500 for permissions matrix | Medium | Runtime environment | Stale backend container and Flyway checksum mismatch on existing Postgres volume. | Call endpoint against old stack without rebuild. | 403 viewer, 200 admin. | 500 from stale runtime. | Rebuild backend + `scripts/repair-flyway-checksums.ps1` if Flyway validate fails. | `auth-smoke.ps1`, Newman 24/24. | Resolved |
