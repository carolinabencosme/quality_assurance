# Modelo de seguridad — borrador (Fase 2)

Keycloak realm: `inventory-realm`

Clients iniciales en `keycloak/realm-export.json`:

- `inventory-frontend` (público, OIDC)
- `inventory-backend` (bearer-only)

Matriz de permisos y policies granulares: ver Plan v3.0 §7.
